// Set VITE_LOGO_URL in your .env to replace the text logo with an image.
// The URL must be publicly accessible (e.g. Cloudinary, S3).
// Recommended size: height 32–40px, transparent background.
export const LOGO_URL: string = import.meta.env.VITE_LOGO_URL ?? ''
